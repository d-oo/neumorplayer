// old-src/src/components/SearchMusic.js 를 대체할 자리.
// TODO: useInfiniteQuery + supabase.from("tracks").select().range(...) 로
// 제목/아티스트/태그 검색 + 정렬(recentAdd/recentPlay/mostPlay)을 구현하세요.
export default function SearchPage() {
  return (
    <div>
      <h2>검색</h2>
      <p>여기에 트랙 검색/목록 UI가 들어갑니다.</p>
    </div>
  );
}
